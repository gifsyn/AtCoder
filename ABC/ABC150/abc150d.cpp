#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    int N, M;
    cin >> N >> M;
    vector<ll> a(N);
    for(int i=0; i<N; i++){
        cin >> a[i];
    }

    

    return 0;
}