
#include <cstdio>

int main(){
    int n;
    long long int x[n], c[n], v[n];
    for(int i = 0; i < n; i++){
        scanf("%lld",&x[i]);
    }
    for(int i = 0; i < n; i++){
        scanf("%lld",&c[i]);
    }
    for(int i = 0; i < n; i++){
        scanf("%lld",&v[i]);
    }

    

    return 0;
}